// Example script demonstrating the binary datatype
// This datatype is useful for handling binary data like images, files, etc.

print "=== Binary Datatype Examples ===";
print "";

// Example 1: Create binary data from Base64 (datatype function)
print "1. Creating binary from Base64 string:";
var binaryData: binary = binary.fromBase64("SGVsbG8gV29ybGQ=");
print "   Created binary data from base64";
print "   Note: binary.fromBase64() is a datatype function";
print "";

// Example 2: Convert binary to string (datatype function)
print "2. Converting binary to string:";
var textBin: binary = binary.fromBase64("SGVsbG8gV29ybGQ=");
var textStr: string = binary.toString(textBin);
print "   Text from binary: " + textStr;
print "   Note: binary.toString() is a datatype function";
print "";

// Example 3: Access length property (variable chain)
print "3. Accessing length property:";
var data: binary = binary.fromBase64("SGVsbG8gV29ybGQ=");
var len: int = data.length;
print "   Data length: " + len + " bytes";
print "   Note: .length is a property accessed on the variable";
print "";

print "=== Datatype Functions vs Variable Chain Functions ===";
print "";
print "DATATYPE FUNCTIONS (callable as binary.*):";
print "  - binary.fromBase64(string) -> binary";
print "  - binary.toString(binary, encoding?) -> string";
print "";
print "VARIABLE CHAIN FUNCTIONS (to be called on variable):";
print "  - var.length -> int (property, already works)";
print "  - var.get(index) -> byte (requires method support)";
print "  - var.set(index, value) (requires method support)";
print "  - var.slice(start, end?) -> binary (requires method support)";
print "  - var.concat(other) -> binary (requires method support)";
print "  - var.toBase64() -> string (requires method support)";
print "";

print "=== All binary datatype examples completed ===";

