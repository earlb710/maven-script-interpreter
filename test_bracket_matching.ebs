// Test script for bracket matching feature

// Test case 1: Simple matching brackets
var test1 string = "test { matching } brackets"

// Test case 2: Nested brackets
if (true) {
    var nested string = "inside { nested [ brackets ] }"
}

// Test case 3: Multiple bracket types
var array = [1, 2, 3, (4 + 5), {key: "value"}]

// Test case 4: Unmatched opening bracket
var broken1 string = "missing close {"

// Test case 5: Unmatched closing bracket  
var broken2 string = "extra close }"

// Test case 6: Complex nested structure
function testFunc(param1 string, param2 int) return bool {
    if (param2 > 0) {
        var result = [param1, (param2 * 2)]
        return true
    } else {
        return false
    }
}

print("Bracket matching test script loaded successfully")
