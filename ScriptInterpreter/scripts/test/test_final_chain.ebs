// Test chained property access as requested in comment

// Simple test: employees[0].department.name="ABC"
var employees:array.record{
    id: int,
    name: string,
    department: record {
        name: string,
        location: string
    }
};

employees = [
    {
        "id": 101,
        "name": "John Doe",
        "department": {
            "name": "Engineering",
            "location": "Building A"
        }
    }
];

print "Test: employees[0].department.name = 'ABC'";
employees[0].department.name = "ABC";
print "Result:";
print employees[0];
print "";
print "SUCCESS! Chained property access works.";
