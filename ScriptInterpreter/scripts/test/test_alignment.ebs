// Test script for contentAlignment and itemAlignment properties
// This test verifies that the new alignment properties are properly defined and accessible
print("Testing alignment properties");
print("============================");

// Create a simple screen to test alignment properties
screen testScreen = {
    "sets": [{
        "setname": "main",
        "vars": [
            {"name": "leftText", "datatype": "string", "value": "Left"},
            {"name": "centerText", "datatype": "string", "value": "Center"},
            {"name": "rightText", "datatype": "string", "value": "Right"}
        ]
    }],
    "areas": [{
        "name": "mainArea",
        "type": "vbox",
        "spacing": "10",
        "padding": "20",
        "items": [
            {
                "varRef": "leftText",
                "display": {
                    "type": "textfield",
                    "labelText": "Content Left Aligned:",
                    "maxLength": 20
                },
                "contentAlignment": "left"
            },
            {
                "varRef": "centerText",
                "display": {
                    "type": "textfield",
                    "labelText": "Content Center Aligned:",
                    "maxLength": 20
                },
                "contentAlignment": "center"
            },
            {
                "varRef": "rightText",
                "display": {
                    "type": "textfield",
                    "labelText": "Content Right Aligned:",
                    "maxLength": 20
                },
                "contentAlignment": "right"
            }
        ]
    }]
};

print("Screen defined with contentAlignment properties");
print("To see the alignment effects, use the interactive console:");
print("  show screen testScreen;");
print("");
print("To test property access:");
print("  call scr.setProperty(\"testscreen.leftText\", \"contentAlignment\", \"center\");");
print("  var align: string = scr.getProperty(\"testscreen.centerText\", \"contentAlignment\");");
print("");
print("Test completed!");
