// Test that valid record assignments still work after validation enhancement

print "Test: Valid record assignment (should work)";
var product:record{sku:string, price:double, inStock:bool};
product = {"sku": "XYZ789", "price": "49.99", "inStock": "Y"};

print "Product assigned successfully:";
print product;

print "";
print "Test: Array of records with valid fields (should work)";
var items:array.record{id:int, name:string, quantity:int};
items = [
    {"id": 1, "name": "Item A", "quantity": 10},
    {"id": 2, "name": "Item B", "quantity": 5}
];

print "Items assigned successfully:";
print items;
