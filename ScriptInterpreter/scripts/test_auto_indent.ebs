// Test auto-indent feature
// When pressing Enter, the new line should auto-indent to match the previous line

myFunction() return int {
    var int x = 10
    var int y = 20
    if x < y then
        print("x is less than y")
    else
        print("x is greater than or equal to y")
    
    return x + y
}

// Test nested blocks
anotherFunction() {
    var int i = 0
    while i < 5 do
        print("Iteration: " + string.from(i))
        i = i + 1
}
