// Quick test to verify chess image path works
print "Testing chess image path resolution...";

var imgPath: string = "../../src/main/resources/images/chess/";
var testFile: string = imgPath + "black_rook.svg";

print "Trying to load: " + testFile;

var fileExists: bool = call file.exists(testFile);
print "File exists: " + fileExists;

if fileExists then {
    print "SUCCESS: Chess image path is correct!";
} else {
    print "ERROR: Chess image path is incorrect.";
    print "Current context would resolve to a path that doesn't exist.";
}
