// Test script for custom function highlighting

// Define a custom function
myCustomFunction() {
    print "Hello from myCustomFunction"
}

// Define another custom function with parameters
calculateSum(a int, b int) return int {
    return a + b
}

// Define a function with the function keyword
function anotherFunction {
    print "This is another function"
}

// Now call the defined functions - these should be highlighted as custom functions
myCustomFunction()
#myCustomFunction

var result int = calculateSum(5, 10)
print result

#anotherFunction

// Call an undefined function - this should be highlighted in red with underline
undefinedFunction()
#undefinedFunction

// Call a builtin - this should use builtin color
print "Using builtin: " + string.toUpper("test")
json.parse("{\"key\": \"value\"}")

// Mixed case test
MyCustomFunction()  // Should still be recognized (case insensitive)
